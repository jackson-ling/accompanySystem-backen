<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.accompany.mapper.LoginMapper">
    <update id="resetPassword">
        update sys_user set password = #{password} where phone = #{phone}
    </update>

    <select id="selectUser" resultType="com.accompany.entity.SysUser">
        select * from sys_user where phone = #{phone}
    </select>

    <insert id="registerUser" useGeneratedKeys="true" keyProperty="id" >
        insert into sys_user (nickname, avatar, phone, password, balance, user_type, gender, status, create_time, update_time)
        values (#{nickname}, #{avatar}, #{phone}, #{password}, 0, 1, 0, 1, now(), now())
    </insert>

</mapper>