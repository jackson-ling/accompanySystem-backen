3-13 号

开发进度

1. 完成项目基本框架构建，所有数据库实体类均根据数据表设计创建了
2. apifox 认证模块的四个接口均已实现，且通过 apifox 测试，还未进行前后端联调

前后端联调发现的问题：返回异常信息时会出现两次弹窗（待修复）

3-14 号（S）

1. 完成用户模块和就诊人模块
2. apifox 认证模块的四个接口均已实现，且通过 apifox 测试，还未进行前后端联调

3-14 号（S）

1. 完成服务模块、陪诊师模块、陪诊师工作台模块、订单模块
2. apifox 认证模块的四个接口均已实现，且通过 apifox 测试，还未进行前后端联调

数据问题：不应把状态 2 对应 2 个状态已接单和服务中，建议往后面加个 7 对应服务中，2 就是已接单

3-15 --- 3-17（W）

1. 修复订单状态定义问题，新增状态 7（服务中），明确订单状态流转：0-待支付 → 1-待接单 → 2-已接单 → 7-服务中 → 3-已完成

2. 修复跨域问题：前后端分离部署时（前端 localhost:5173，后端 localhost:8080），在 WebMvcConfig 中配置 CORS，允许跨域访问

3. 完成收藏模块：实现收藏陪诊师、取消收藏、获取收藏列表功能，包含陪诊师基本信息（姓名、头像、简介）

4. 完成消息聊天模块：
   - 会话管理：获取会话列表、未读消息数、标记已读、删除会话
   - 聊天功能：发送消息、获取聊天记录、删除聊天记录（支持客服和陪诊师两种类型）
   - 优化聊天界面显示，删除聊天记录时显示提示语"聊天记录已清空"

5. 优化陪诊师模块：
   - 允许未登录用户浏览陪诊师列表和详情
   - 收藏功能需要登录验证
   - 修复可用时间查询的日期格式问题（支持 M-dd/MM-dd 格式输入）

6. 新增测试数据：包含 18 张表的完整测试数据，覆盖用户、陪诊师、订单、消息、充值等全部模块

7. 新增 BasicEnum 枚举：PARAM_ERROR、ALREADY_FAVORITED、NO_PERMISSION、CONVERSATION_NOT_EXIST

8. 修复支付流程：前端调用支付成功回调后，后端正确创建订单并更新状态

9. 优化了大量的前端兼容性

10. 移除了 favorite表的type字段，在拿到项目后先执行迁移-favorite-type 字段文件

3-18 号（S）

1. 完成充值模块、钱包模块、字典模块、反馈模块
2. apifox 认证模块的四个接口均已实现，且通过 apifox 测试，还未进行前后端联调

剩余最后2个模块（上传模块和AI模块）需提供AI模型，以及知晓图片如何存储且采用什么存储才可以继续完善
